# P5.3 Gate — Plan-aware organization onboarding

**PHASE_ID**: P5.3
**STATUS**: PASS (local verification)
**DEPENDENCY**: P5.2 PASS.
**DATABASE/MIGRATION**: N/A; the existing schema supports plan, areas, memberships and optional Employee links.

## Evidence

| Criterion | Result | Evidence |
|---|---|---|
| Root cause | PASS | `src/App.tsx` no longer treats a valid OWNER without Employee as incomplete. |
| Plan selection/persistence | PASS | `OnboardingChoiceModal`, `api/onboarding/onboarding.js`; invalid plans rejected; plan is in the initial organization INSERT. |
| OWNER validity | PASS | Owner-only/no-area/no-admin component and API tests. |
| OWNER Employee | PASS | Explicit opt-in, active linked Employee, optional area. |
| ADMIN optional | PASS | Team-only step; existing User reuse and one-time temporary credentials for new Users. |
| Areas optional | PASS | Zero areas creates no Area rows; duplicate names rejected. |
| Atomicity | PASS | `sql.transaction(queries)` and simulated failure test. |
| Entitlement/security | PASS | `PLAN_IDS` backend whitelist; Personal/Free reject Team-only structures; no billing endpoint/state. |
| Accessibility/i18n | PASS | Wizard progress, labelled fields, focus errors, ES/EN keys and component tests. |
| Regression | PASS | Focused App, onboarding and backend tests; full suite/build/lint required at final handoff. |

## Compact E2E policy

P5.3 uses the smallest browser coverage that observes the critical journey: one Team-minimal /
owner-only browser path in a single worker. Personal, Team-full, invalid-plan, existing-admin, area
cardinality and transaction-failure combinations remain unit/integration/API contracts. This keeps
the E2E contract at 1/1 PASS in 27.5 s without dropping the required domain checks. No production
E2E, push, deploy or migration is allowed.

## Gate

`PHASE_P5.3_GATE = PASS` after the focused browser smoke (1/1, 27.5 s), full local test suite
(143 files / 1268 tests), lint, typecheck and build passed. No migration or production access was
used. P6 remains not started.

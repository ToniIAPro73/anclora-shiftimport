# R3-M15 — E2E

STATUS: DONE — PASS

## 1. Objetivo
Suite E2E cubriendo el flujo completo de Scheduling: crear draft → editar assignments → validaciones (overlap/rest-rule) → publicar → nueva versión → historial, en navegador real contra la API real.

## 2. Problema que resuelve
Ninguna microfase individual de R3 verifica el flujo completo de punta a punta; esta lo hace, siguiendo el patrón browser→API→auth→DB→response→UI exigido por el prompt maestro (§20).

## 3. Estado actual del repositorio
IMPLEMENTED. Depende de R3-M00..M14 completas.

## 4. Alcance IN
- E2E: login PLANNER → crear draft → añadir assignments → intentar overlap (rechazado) → intentar violar rest-rule (rechazado) → publicar → verificar turnos visibles en dashboard existente → crear nueva versión → ver historial.
- E2E negativo: EMPLOYEE intenta acceder a endpoints de escritura de Scheduling → rechazado.
- E2E cross-tenant: usuario de organización A no ve schedules de organización B.

## 5. Alcance OUT
Performance/carga — fuera de alcance MVP.

## 6. Dependencias
R3-M00..M14.

## 7. Decisiones arquitectónicas
Reutiliza la infraestructura E2E ya existente en `qa/e2e-acceptance/` (Playwright-style, ya usada para Safe Import) en lugar de introducir un framework E2E nuevo (§32 del prompt maestro: no sustituir stack por preferencia personal).

## 8. Modelo de datos afectado
N/A — solo tests.

## 9. API / Backend
N/A — solo tests contra la API existente.

## 10. Frontend / UX
N/A — solo tests contra la UI existente.

## 11. Seguridad y autorización
Cubierto por los casos negativos del Alcance IN.

## 12. i18n
E2E corre en ambos idiomas al menos para el flujo feliz principal (ES y EN), consistente con `qa/public-header-auth/` ya existente.

## 13. Accesibilidad
N/A adicional — cubierto en R3-M09.

## 14. Responsive / temas
E2E del flujo feliz en al menos un breakpoint desktop y uno móvil, dark y light, consistente con el resto de la suite QA existente.

## 15. Observabilidad / errores
E2E verifica también los mensajes de error mostrados al usuario en los casos negativos (overlap, rest-rule, permisos), no solo el código HTTP.

## 16. Migraciones
N/A.

## 17. Compatibilidad y datos existentes
N/A.

## 18. Tasks

### T01 — E2E flujo feliz completo
Objetivo: cubrir el flujo completo descrito en Alcance IN.
Archivos / módulos probables: `qa/e2e-acceptance/specs-local/scheduling-e2e.spec.ts` (nuevo, siguiendo convención de `format-memory.spec.ts` ya existente).
Cambios: nueva spec E2E.
No hacer: no mockear la API — E2E real contra el backend.
Criterios de aceptación:
- [x] Flujo completo pasa en ES y EN, dark y light, desktop y móvil.
Tests: la spec es el entregable.
Evidencia esperada: reporte de ejecución E2E adjunto.

### T02 — E2E casos negativos (permisos, cross-tenant)
Objetivo: cubrir los casos negativos del Alcance IN.
Archivos / módulos probables: misma spec o una hermana `scheduling-authz.spec.ts`.
Cambios: nueva spec E2E.
No hacer: N/A.
Criterios de aceptación:
- [x] EMPLOYEE rechazado en endpoints de escritura, verificado en UI y no solo en API.
- [x] Cross-tenant verificado end-to-end.
Tests: la spec es el entregable.
Evidencia esperada: reporte de ejecución E2E adjunto.

## 19. Tests obligatorios
`E2E`.

## 20. Evidencias

- `scheduling-e2e.spec.ts` y `scheduling-authz.spec.ts` commiteadas.
- Dos ejecuciones consecutivas de ambas specs: `4/4` PASS en cada ejecución,
  `8/8` acumulados.
- Flujo feliz validado en browser real con API y Neon dev: draft, edición,
  overlap `422` visible, rest-rule `422` visible, publicación, dashboard,
  nueva versión e historial.
- Variantes verificadas: ES/dark/desktop y EN/light/móvil.
- Regresión E2E focalizada de Safe Import + M14 + Scheduling existente:
  `14/14` PASS.
- Remediación durante M15: corregido el import relativo de
  `api/schedules/[scheduleId]/versions/index.js`, que impedía cargar el
  endpoint real de historial.

## 21. Gate
Gates requeridos: **G11** (E2E), **G13** (Regression — verificar que la suite E2E existente de Safe Import sigue en PASS tras toda la integración de R3-M14).

Resultado: **PASS**.

- G11 — PASS: ambas specs E2E reales, dos ejecuciones consecutivas, `8/8`.
- G13 — PASS: regresión focalizada Safe Import + M14 + Scheduling existente,
  `14/14`.

## 22. Rollback / remediación
Si el E2E revela un gap funcional real (no solo un flake de test): la remediación va en la microfase original correspondiente (R3-M04..M14), esta microfase no implementa fixes de dominio, solo los detecta y referencia.

## 23. Criterio de DONE
Ambas specs E2E en PASS de forma estable, verificado con dos ejecuciones
consecutivas; la suite E2E existente de Safe Import sigue en PASS; Gates G11 y
G13 en PASS.
